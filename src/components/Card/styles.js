import css from 'styled-jsx/css';

export default css`
  .card {
    background: #ffffff;
    box-shadow: 0px 12px 34px rgba(0, 0, 0, 0.1);
    border-radius: 4px;

    &.rounded {
      border-radius: 12px;
    }
  }
`;
